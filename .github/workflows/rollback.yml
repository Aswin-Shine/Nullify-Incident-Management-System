# # =============================================================
# # Nullify — Manual Rollback Workflow
# # Trigger: GitHub UI → Actions → Run workflow → enter tag
# # =============================================================

# name: Rollback

# on:
#   workflow_dispatch:
#     inputs:
#       image_tag:
#         description: "Image tag to roll back to (e.g. a1b2c3d)"
#         required: true
#         type: string
#       confirm:
#         description: "Type ROLLBACK to confirm"
#         required: true
#         type: string

# permissions:
#   contents: read

# jobs:
#   rollback:
#     name: Rollback → ${{ inputs.image_tag }}
#     runs-on: ubuntu-latest
#     timeout-minutes: 10

#     environment:
#       name: production

#     steps:
#       - name: Validate confirmation
#         run: |
#           if [ "${{ inputs.confirm }}" != "ROLLBACK" ]; then
#             echo "Confirmation text incorrect. Aborting."
#             exit 1
#           fi

#       - name: Set up SSH
#         run: |
#           mkdir -p ~/.ssh
#           echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/nullify_deploy
#           chmod 600 ~/.ssh/nullify_deploy
#           ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts 2>/dev/null

#       - name: Rollback on EC2
#         env:
#           EC2_HOST:  ${{ secrets.EC2_HOST }}
#           EC2_USER:  ${{ secrets.EC2_USER }}
#           IMAGE_TAG: ${{ inputs.image_tag }}
#           DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
#         run: |
#           ssh -i ~/.ssh/nullify_deploy \
#               -o StrictHostKeyChecking=no \
#               ${EC2_USER}@${EC2_HOST} << 'REMOTE'

#             set -euo pipefail
#             cd /opt/nullify

#             echo "=== Rolling back to tag: ${IMAGE_TAG} ==="

#             # Override image tags via env
#             export BACKEND_IMAGE="${DOCKERHUB_USERNAME}/nullify-backend:${IMAGE_TAG}"
#             export FRONTEND_IMAGE="${DOCKERHUB_USERNAME}/nullify-frontend:${IMAGE_TAG}"

#             docker compose pull backend frontend
#             docker compose up -d --no-deps backend frontend

#             echo "=== Verifying rollback ==="
#             sleep 8
#             curl -sf http://localhost:8000/health && echo "Backend ✓"
#             curl -sf http://localhost/health && echo "Frontend ✓"

#             echo "=== Rollback complete to ${IMAGE_TAG} ==="

#           REMOTE

#       - name: Cleanup SSH key
#         if: always()
#         run: rm -f ~/.ssh/nullify_deploy

#       - name: Rollback summary
#         run: |
#           echo "## ⏪ Rollback Complete" >> $GITHUB_STEP_SUMMARY
#           echo "| Item | Value |" >> $GITHUB_STEP_SUMMARY
#           echo "|------|-------|" >> $GITHUB_STEP_SUMMARY
#           echo "| Rolled back to | \`${{ inputs.image_tag }}\` |" >> $GITHUB_STEP_SUMMARY
#           echo "| Triggered by | ${{ github.actor }} |" >> $GITHUB_STEP_SUMMARY
